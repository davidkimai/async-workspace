# Async Collaboration Workspace

## Get it on your machine (only setup step)

```bash
git clone https://github.com/davidkimai/async-workspace.git
cd async-workspace
```

If the folder already exists, open it directly.

## Optional agent prompt (also only clone)

Copy this to your agent if you want it done for you:

```text
Clone this repo locally at https://github.com/davidkimai/async-workspace.git.
If it already exists locally, just open it.
No other file changes.
```